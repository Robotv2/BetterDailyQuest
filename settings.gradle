plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'BetterDailyQuest'

def localAnchor = file('../Anchor')
if (localAnchor.isDirectory() && new File(localAnchor, 'settings.gradle').isFile()) {
    includeBuild(localAnchor)
}
